<div class="item" data-directory="{{directory}}">
    <img src="{{{image}}}" />
    <h1>{{title}}</h1>
    <p>{{description}}</p>
    <button class="playDiskButton">Play</button>
    <button class="editDiskButton">Edit</button>
    <input class="blurAmount" type="range" min="1" max="48" value="{{blur_amt}}">
</div>