[Unit]
Description=IPFS daemon
After=network.target

[Service]
EnvironmentFile=/usr/src/app/core/dsharing/IPFS_ENV
ExecStart=/usr/bin/ipfs daemon --init --enable-gc
Restart=on-failure

[Install]
WantedBy=default.target
